import React from "react";
import styles from "./UserProfile.module.css";

const UserProfile = () => (
  <div className={styles.UserProfile}>UserProfile Component</div>
);

export default UserProfile;
